name: snapcraft-docs-flutter-app
version: '1.0'
summary: Super Cool App
description: Super Cool App that does everything!
base: core22
confinement: strict
grade: stable

apps:
  snapcraft-docs-flutter-app:
    command: snapcraft_docs_flutter_app
    extensions: [gnome]

parts:
  snapcraft-docs-flutter-app:
    source: .
    plugin: flutter
    flutter-target: lib/main.dart
